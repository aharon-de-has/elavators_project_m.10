import pygame
import queue

class Elevator():
    def __init__(self):
        self.__current_floor = 0
        self.__still_available = 0
        # self.__request = set()

    
class Floor():
    def __init__(self, num):
        self.__floor_num = num
        mouse
        timer

        
class Building:
    def __init__(self, floors: int, elevators: int):
        self.__floors = []
        for i in range(floors):
            self.__floors.append(Floor(i))
        self.__elevators = []
        for i in range(elevators):
            self.__elevators.append(Elevator(i))
        self.__request = queue.Queue()

    def control(self, floor_num): 
        self.__request.put(floor_num)
        neareste = get_neareste_elevator()
        if elvator == 0:
            remove
        # return eleator,timer


    def get_neareest_elevator(self, ):

    
    




